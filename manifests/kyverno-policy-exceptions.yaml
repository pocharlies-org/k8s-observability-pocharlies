---
apiVersion: kyverno.io/v2
kind: PolicyException
metadata:
  name: monitoring-privileged-node-exporters
  namespace: monitoring
  annotations:
    sre.edani.com/reason: "DGX node exporters need host namespace access for read-only probes; opencode-node-ops uses it for one authenticated fixed service restart on ubuntu."
spec:
  background: true
  exceptions:
    - policyName: disallow-privileged-containers
      ruleNames:
        - no-privileged
        - autogen-no-privileged
  match:
    any:
      - resources:
          kinds:
            - Pod
          namespaces:
            - monitoring
          names:
            - dgx-gpu-mem-exporter-*
            - dgx-node-exporter-cpu-*
            - opencode-node-ops-*
      - resources:
          kinds:
            - DaemonSet
          namespaces:
            - monitoring
          names:
            - dgx-gpu-mem-exporter
            - dgx-node-exporter-cpu
            - opencode-node-ops
